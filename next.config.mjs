/** @type {import('next').NextConfig} */
import { withNetlify } from '@netlify/next';

const nextConfig = {
  // Add any Next.js config options you need here
};

export default withNetlify(nextConfig);
